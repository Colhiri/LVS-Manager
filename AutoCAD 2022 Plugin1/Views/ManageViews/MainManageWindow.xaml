<Window x:Class="AutoCAD_2022_Plugin1.Views.ManageViews.MainManageWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:AutoCAD_2022_Plugin1.ViewModels.ManageVM"
             xmlns:views="clr-namespace:AutoCAD_2022_Plugin1.Views.ManageViews"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainManageVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="ManageLayout" DataType="{x:Type viewmodels:ManageLayoutVM}">
                <views:ManageLayoutView/>
            </DataTemplate>
            <DataTemplate x:Key="Template2" DataType="{x:Type viewmodels:ManageVIewportVM}">
                <views:ManageViewportView/>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    
    <StackPanel>
        <!-- <TabControl Name="Managing" Margin="2" MinHeight="50" MinWidth="100">                                        -->
        <!--     <TabItem Name="LayoutEdit" Header="Макет" Width="{Binding ElementName=ViewportEdit, Path=ActualWidth}">  -->
        <!--         <views:ManageLayoutView/>                                                                            -->
        <!--     </TabItem>                                                                                               -->
        <!--                                                                                                              -->
        <!--     <TabItem Name="ViewportEdit" Header="Видовой экран">                                                     -->
        <!--         <views:ManageViewportView/>                                                                          -->
        <!--     </TabItem>                                                                                               -->
        <!-- </TabControl>                                                                                                -->

        <TabControl ItemsSource="{Binding tabs}" ContentTemplate="{StaticResource ManageLayout}">
        </TabControl>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            
            <Button Margin="2" Name="btn_Cancel">
                Отмена
            </Button>
        </StackPanel>

    </StackPanel>
</Window>
